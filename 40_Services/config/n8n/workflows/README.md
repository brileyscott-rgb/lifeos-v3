# n8n Workflow Placeholders

Future reviewed workflow exports belong here as text/config artifacts after credentials and backup decisions are settled.

Do not store active credentials, webhook secrets, API keys, bot tokens, or execution data in this directory.

Initial workflow targets:

- `watch-folder-capture`
- `webhook-capture`
- `approval-routing`
- `workflow-failure-alerts`
- `event-log-append`
- `daily-weekly-digests`
