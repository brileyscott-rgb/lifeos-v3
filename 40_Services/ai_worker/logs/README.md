# AI Worker Logs

This directory will contain operational logs when the AI worker is active.

## Current Status

No logs directory active. The worker runs in dry-run mode with stdout logging only.

## Future

- Log files per job
- Error logs
- Execution traces

Log files are gitignored.
