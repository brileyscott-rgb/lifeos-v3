# Test Payloads

Example Telegram update payloads for manual testing or future automated tests.

Files:

- `capture_update.example.json` — Simulated `/capture` update
- `p_queue.example.json` — Simulated `/p` list-pending update
- `view_item.example.json` — Simulated `/view 1` update
- `approve_item.example.json` — Simulated `/a 1` update
- `reject_item.example.json` — Simulated `/r 1` update
- `approve_latest.example.json` — Simulated `/a latest` update
- `reject_latest.example.json` — Simulated `/r latest` update
- `approve_solo.example.json` — Simulated `/a` (no arg, single pending) update
- `reject_solo.example.json` — Simulated `/r` (no arg, single pending) update
